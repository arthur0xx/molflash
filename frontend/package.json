{
  "name": "store-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "generate:catalog": "node scripts/generate-static-catalog.mjs",
    "build:preview": "npm run generate:catalog && VITE_STATIC_CATALOG=true vite build",
    "build:pages": "npm run generate:catalog && VITE_STATIC_CATALOG=true vite build --base=/molflash/",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
