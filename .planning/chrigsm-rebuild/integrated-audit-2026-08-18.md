# تدقيق تكاملي — ChriGsm

**التاريخ:** 2026-08-18 GMT+1  
**الفرع:** `rebuild/next-firebase`  
**النطاق:** الحساب، CMC، الكتالوج، الطلب، الدفع اليدوي، المحفظة، رفع إثبات التحويل، الدعم، الإشعارات، وقواعد Firestore.

## العطل المؤكد والإصلاح

| الحالة | النتيجة |
|---|---|
| العطل | لم تظهر وسيلة `Cashplus` المفعلة للعميل في شحن الرصيد، وعرضت الواجهة خطأ تحميل الوسائل. |
| سبب الجذر | استعلام `GET /api/payments` جمع `where(status == active)` مع `orderBy(sortOrder)`، وهو اعتماد على فهرس مركب لا يوجد في Firestore. |
| البيانات الفعلية | وثيقة `paymentMethods/7PZoYC8O1nSkNpKsCJXJ` موجودة، وحالتها `active` ونطاقها `wallet_topup` وترتيبها `1`. |
| الإصلاح | إزالة `orderBy` الخادمي من الاستعلام، ثم ترتيب الوسائل المفعلة محليًا حسب `sortOrder`. لا يغيّر ذلك صلاحيات الوصول أو بيانات الدفع. |
| أثر الإصلاح | يستطيع مسار العميل قراءة وسيلة Cashplus المفعلة من دون انتظار إنشاء فهرس Firestore يدوي. |

## نتائج التدقيق

| المجال | التحقق | النتيجة |
|---|---|---|
| حساب العميل | حساب `yinarthur.0x@gmail.com` له ملف Firestore بدور `customer`، وثُبتت مطالبة Firebase Auth إلى `role=customer`. | سليم؛ لا يملك CMC. |
| حراسة CMC | `AdminAccessGate` يسمح فقط بـ`admin` و`manager` بعد تجديد الجلسة. | سليم. |
| قواعد Firestore | فحص المحاكي غطى الزائر، عزل العملاء، العميل المحظور، قراءة دفع المالك فقط، ومنع الكتابات المباشرة للدفع والمحفظة وCMC. | ناجح. |
| الطلب | السعر يقرأ خادميًا من الخدمة النشطة، والحقول تتحقق من المخطط الخادمي، وينشأ الطلب مع سجل تدقيق. | سليم. |
| الدفع اليدوي | إنشاء المرجع يقيّد الوسيلة النشطة والنطاق والملكية؛ الاعتماد يتطلب إثبات التحويل والمالك وينفذ Transaction. | سليم. |
| الدعم | العميل لا يقرأ إلا تذاكره، والإنشاء يتحقق من المدخلات ويحفظ سجل تدقيق. | سليم. |
| الصور وإثبات الدفع | تواقيع Cloudinary تصدر خادميًا؛ إثبات الدفع لا يفتح إلا للمالك برابط موقّع. | سليم. |
| WhatsApp | Webhook يتحقق من HMAC؛ إشعارات الطلبات وإثبات التحويل تفشل بأمان عندما لا تتوفر تهيئة Meta الرسمية. | صحيح وظيفيًا، لكنه غير مفعل فعليًا حتى تهيئة Meta الرسمية. |
| الأسرار | لم يظهر مفتاح فعلي ضمن Git؛ الموجود `.env.example` وقيم Firebase عامة للمتصفح. | سليم. |

## بوابات الجودة المنفذة

```text
npm run lint                 PASS
npm run build                PASS
npm run test:firestore-rules PASS
npm audit --omit=dev         لا توجد ثغرات عالية أو حرجة؛ توجد 6 ثغرات متوسطة موروثة عبر `firebase-admin`/`@google-cloud/storage`، والإصلاح المقترح كسرٌ محتمل لذا لم يطبق قسريًا.
git diff --check             PASS قبل الالتزام
```

## حدود مقصودة

لا يتم تفعيل CMI أو Payzone أو أي دفع إلكتروني أو رسالة WhatsApp فعلية من دون أسرار خادمية وقوالب Meta رسمية. لا يقوم المرجع أو الصورة بتأكيد الدفع تلقائيًا؛ يتطلب شحن الرصيد أو بدء الطلب مراجعة مالك موثقة.

## الإجراء التالي

الالتزام بإصلاح تحميل وسائل الدفع، دفعه إلى فرع إعادة البناء، انتظار معاينة Vercel، ثم فحص الصفحة العامة ومسار الدفع غير الموثق. بعد نشر المعاينة يختبر المالك شحن الرصيد بحساب العميل؛ ينبغي أن تظهر Cashplus مباشرة عند فتح «شحن الرصيد».
