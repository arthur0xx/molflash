# سجل التقدم — إعادة بناء ChriGsm

## 2026-08-15 — ضبط بنية الكتالوج في المعاينة

- فُتحت معاينة CMC المرتبطة ببيانات Firebase الحقيقية في `https://chrigsm-next-7sc0vmpc0-arthurs-projects-713d8d1f.vercel.app/admin` بجلسة المدير الحالية.
- أُنشئت وحُفظت أربعة تصنيفات نشطة من خلال CMC: **خدمات Server**، **الأدوات**، **كراء الأدوات**، و**متنوع**؛ وكان تصنيف **أدوات التفعيل** موجودًا مسبقًا.
- أصبح هيكل التصنيفات الفعلي: خدمات Server، الأدوات، كراء الأدوات، متنوع، أدوات التفعيل.
- حُفظت التصنيفات عبر المسار الخادمي للـCMC، لا عبر حالة محلية أو بيانات تجريبية.
- فُحص نموذج إنشاء خدمة في تصنيف خدمات Server. الحقول المطلوبة تشمل الاسم، slug، التصنيف، السعر بالدرهم المغربي، التسليم، الوصف، وحالة الإتاحة. لا يدعم النموذج الحالي إنشاء الحقول الديناميكية عند إضافة خدمة جديدة؛ تنشأ الخدمة مع `fields: []`.
- **العائق الحالي:** لا يمكن اعتماد خدمات قابلة للبيع من دون أسعار وتسليمات يحددها المالك أو مصدر رسمي؛ لذلك لم تُنشأ خدمات عامة بأسعار افتراضية أو وهمية.

## الخطوة التالية

الحصول على قائمة الخدمات المعتمدة مع السعر بالدرهم المغربي وطريقة التسليم لكل خدمة، ثم إنشاء الخدمات نشطة فقط بعد التحقق من البيانات. أي خدمات تحتاج حقول طلب مخصصة (مثل IMEI أو البريد) تتطلب إضافة هذه الحقول عبر مسار إداري مدعوم أو تطوير واجهة CMC قبل تفعيلها.

## 2026-08-15 — إصلاح توافق الواجهة وبطاقات CMC

عولجت بطاقات الخدمات العامة بحيث تصبح **كل البطاقة رابطًا واحدًا واضحًا** إلى تفاصيل الخدمة، مع مؤشر تركيز لوحة المفاتيح وعدم وجود زر صغير هو منطقة النقر الوحيدة. وعولجت مجلدات منتجات CMC بحيث يغطي زر الفتح كامل مساحة البطاقة تقريبًا، مع هدف لمس لا يقل عن 86–92 بكسل بحسب العرض.

أضيفت طبقة CSS تكيفية للـCMC. بين الشاشات المتوسطة والهواتف، تتحول شبكة العملاء إلى بطاقة منظمة وحقول الرصيد إلى صفوف قابلة للقراءة؛ وعلى العرض الضيق تتحول نافذة الإضافة/التعديل إلى bottom sheet بعرض كامل وحقول متتابعة وأزرار حفظ/إلغاء ثابتة في الأسفل. كما أُعيد تنظيم صفوف التصنيفات والخدمات بحيث لا تضغط الأزرار أو النصوص. لا تغيّر هذه الدفعة مسارات API أو Firebase أو أي بيانات حساسة.

**بوابة البناء:** نجح `npm run build` داخل `next/` في 2026-08-15 بعد تعديل `next/src/app/globals.css` و`next/src/components/service-card.tsx`.

**الحالة:** جاهز للمراجعة، الالتزام، ثم معاينة Vercel قبل أي ترقية إنتاج.

### تحقق المعاينة الأولي

أنشأ Vercel معاينة للالتزام `360d9e2` على الرابط `https://chrigsm-next-3il7ifob4-arthurs-projects-713d8d1f.vercel.app`. فُحصت الصفحة الرئيسية في المعاينة، وظهر رابط واحد دلالي يغطي بطاقة خدمة **Zhizhen Schematics — تفعيل 3 أشهر** كاملةً. النقر على البطاقة أعاد محتوى صفحة تفاصيل الخدمة الصحيح، ما يؤكد إزالة مشكلة حصر النقر في أيقونة صغيرة.

### فحص بصري للهاتف

استُخدم عرض `390×844` على رابط المعاينة المصرح به. ظهرت الترويسة والبحث وشريط التصنيفات وبطاقة الخدمة وشريط التنقل السفلي دون قص للنص أو تمرير أفقي. البطاقة أصبحت منطقة لمس واحدة واضحة، والعناصر الرئيسية داخلها مقروءة: التصنيف، الاسم، الوصف، التسليم، والسعر. لم يُنشر هذا الإصلاح إلى الإنتاج بعد؛ الخطوة التالية هي فحص CMC المعتمد ثم ترقية المعاينة فقط إن اجتاز الفحص.
كما فُحصت المعاينة عند `1440×1000`. بقيت الترويسة والبحث وفئات الكتالوج وبطاقة الخدمة والدعم متباعدة ومقروءة من دون تراجع ناتج عن إصلاح الهاتف. لا توجد مشكلة مرئية جديدة في البطاقة؛ ظهور بطاقة خدمة واحدة فقط يعكس بيانات الكتالوج الحالية وليس مشكلة تخطيط.

### حالة الإنتاج والعائق

المعاينة الجديدة اجتازت البناء والفحص البصري العام، لكن **لم تُرقَّ إلى الإنتاج**. فشل نشر سطر الأوامر لأن اعتماد Vercel المحلي غير متاح، ثم ظهرت في لوحة Vercel جلسة مالك صالحة وخيار `Promote to Production`، لكن اختيار القائمة لم يثبت بعد ثلاث وسائل واجهة مختلفة. تؤكد قائمة النشرات أن آخر التزام ظاهر ما زال معاينة فقط، بينما يبقى الإنتاج على الإصدار السابق السليم. لم أكرر محاولة الترقية بعد ذلك.

**نقطة الاستئناف المطلوبة:** بعد إتاحة ترقية Vercel أو تنفيذها يدويًا، أتحقق من الرابط الدائم ثم أعود مباشرة إلى الكتالوج. الكتالوج متوقف عند اكتمال التصنيفات الخمس؛ الخدمات الجديدة تنتظر أسعارًا معتمدة بالدرهم المغربي وطريقة تسليم وحقول عميل لكل خدمة، كي لا ندخل منتجات أو أسعارًا وهمية.

### طلب المالك لترقية الإنتاج

في 2026-08-15 طلب المالك صراحةً ترقية إصلاحات الواجهة. تأكدت من أن معاينة الالتزام `360d9e2` بحالة `Ready` داخل جلسة Vercel المخولة، مع فتح قائمة إجراءات النشر لاستكمال الترقية إلى الإنتاج.
اختير إجراء `Promote to Production` مباشرةً من قائمة معاينة Vercel بناءً على طلب المالك الصريح؛ يجري الآن التحقق من شاشة التأكيد أو حالة النشر الناتجة.
أُكدت عملية `Promote to Production`، وبدأت Vercel بناء نشر إنتاج جديد باستخدام متغيرات بيئة الإنتاج وربطه بالنطاق الدائم `temporary-speedy-jade-mdelya8.vercel.app`.
أصبح نشر الإنتاج الجديد هو `Latest` وببيئة `Production`، وما زال البناء قيد التنفيذ بعد اجتياز مرحلة التجميع والوصول إلى فحص TypeScript.
تتابع Vercel جمع آثار البناء لنشر الإنتاج، ولم يظهر أي خطأ حتى هذه المرحلة.
اكتمل نشر الإنتاج بنجاح في 48 ثانية بحالة `Ready` للالتزام `360d9e2`، وأُسنِد النطاق الدائم `temporary-speedy-jade-mdelya8.vercel.app` إلى الإصدار الجديد.
### تحقق الإنتاج بعد الترقية

فُتح الرابط الدائم بعد الترقية وظهر الكتالوج والتصنيفات والخدمة الحالية بنجاح. كما فُتحت صفحة تفاصيل خدمة `Zhizhen Schematics — تفعيل 3 أشهر` بالنقر على البطاقة من الصفحة الرئيسية، ما يؤكد أن رابط البطاقة الكامل يعمل في الإنتاج.

**الاستئناف التالي:** إضافة خدمات حقيقية إلى التصنيفات المكتملة بمجرد اعتماد اسم الخدمة وسعرها بالدرهم وطريقة تسليمها وحقول بيانات العميل المطلوبة.

### 2026-08-15 — تحسين الصور والبطاقات ومحررات CMC

أضيفت طبقة واجهة نهائية للبطاقات تجعل رابط البطاقة كامل المساحة، وتعرض صورة الخدمة بعرض البطاقة وارتفاع ثابت كبير مع قص آمن، وتحدّ من الوصف الطويل دون كسر السعر أو التسليم. أضيفت بدائل مرئية عند غياب الصورة ووصف بديل دلالي للصورة.

أُعيد ضبط صفوف المنتجات في CMC لتظهر صورة أكبر وتفاصيل قابلة للقراءة، كما أضيف تنسيق كامل لمكوّن رفع الصور ومعاينته وأزراره. أصبحت نافذة الإضافة والتعديل أعرض على الحاسوب، قابلة للتمرير داخلية، وتتحول إلى bottom sheet منظم على الهاتف مع حقول بعرض كامل وأزرار حفظ/إلغاء واضحة وثابتة.

**بوابة البناء:** نجح `npm run build` بعد التعديلات النهائية.

### 2026-08-15 — صورة الحساب الدائرية الافتراضية

أضيفت دائرة صورة واضحة في رأس منطقة العميل، مع صورة افتراضية محلية تعتمد أيقونة مستخدم عند إنشاء الحساب أو غياب صورة مرفوعة. لا يحتاج الحساب الجديد إلى رابط صورة أو أصل Cloudinary؛ يبقى الحقل فارغًا ويظهر البديل تلقائيًا. عند اختيار العميل صورة جديدة، يستمر الرفع الموقّع الحالي وحفظ النتيجة في ملف العميل، وتظهر الصورة داخل الدائرة، مع استمرار زر إزالة الصورة للعودة إلى الافتراضية.

**بوابة البناء:** نجح `npm run build`، ونجح `git diff --check`، ولم تتغير مسارات Firebase أو قواعد الوصول.
معاينة الالتزام `5ffc4ed` ظهرت في Vercel بحالة بناء، والرابط المتوقع هو `https://chrigsm-next-r0z0vx7q9-arthurs-projects-713d8d1f.vercel.app`. لم يُجرَ الفحص البصري بعد لأن المعاينة تعرض مؤقتًا `Deployment is building`.
أصبحت معاينة `5ffc4ed` جاهزة. فحص `/account` على المعاينة يثبت أن حالة الزائر غير المسجل تعمل وتعرض مسار تسجيل الدخول الصحيح. فحص الدائرة داخل لوحة الحساب الموثقة يحتاج جلسة عميل مسجلة على نطاق المعاينة؛ لم أستخدم بيانات اعتماد أو أنشئ حسابًا جديدًا لهذا الفحص.

### 2026-08-15 — معاينة تطوير مباشرة

كان خادم Next.js التطويري موجودًا أصلًا على المنفذ 3000، لذلك أُتيح عبر الرابط المؤقت `https://3000-ie5kcbbpcvn6j8yko44yq-3afe6446.us3.manus.computer`. ظهر خطأ 500 سببه favicon ICO غير صالح لـ Next.js؛ أُعيد ترميز الملف إلى ICO بصور RGBA، فعاد `/admin` محليًا وعبر الرابط العام بحالة HTTP 200. تظهر لوحة الإدارة للمستخدم غير المسجل في حالة التحقق من صلاحية الإدارة، ولا تُكشف بيانات CMC قبل المصادقة.
