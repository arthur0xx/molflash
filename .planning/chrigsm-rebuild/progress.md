# سجل التقدم

## 2026-08-13 — التهيئة

- تم حفظ قرار البنية: Next.js + Firebase + Vercel.
- تم استبعاد NocoDB من مصدر الحقيقة؛ يُحتفظ به كخيار لوحة داخلية مستقبلية فقط.
- تم تأجيل NestJS إلى مرحلة نمو تتطلب خادمًا منفصلًا أو عمالًا خلفيين كثيفين.
- تم توثيق طبقتين للمعاينة: تحديث محلي فوري أثناء البرمجة ورابط Vercel Preview لكل فرع قبل الإنتاج.
- تم إنشاء ملفات التخطيط المستمر في `.planning/chrigsm-rebuild/`.

## العوائق الحالية

- يلزم إنشاء أو ربط مشروع Firebase واحد قبل تنفيذ مصادقة وFirestore وStorage.
- يلزم تأكيد واحد من المالك لتفويض إعادة البناء المستقلة وعدم طلب قرارات تصميم/بيانات إضافية حتى تسليم النسخة التجريبية.

## لا تغييرات إنتاجية بعد

لم يُنشأ مشروع Next.js جديد ولم تُحذف الواجهة الحالية أو بياناتها التجريبية. يبقى الموقع الحالي رابطًا مرجعيًا حيًا إلى أن يصبح البديل جاهزًا للمراجعة.

## 2026-08-13 — إنشاء Firebase

تم فتح Firebase Console بحساب Google المسجل وبدء إنشاء مشروع باسم `ChriGsm Demo` بلا فوترة. أظهر Firebase أن الحساب بلغ حد عدد مشاريع Google Cloud ولا يمكن إنشاء مشروع جديد حاليًا. ظهر مشروع قائم باسم `device-streaming-c14af3c9`، لكنه غير معروف الغرض لذلك لن يُضاف Firebase إليه أو يُعدّل من دون موافقة صريحة. سيستمر بناء التطبيق حول Firebase Emulator وبيانات تجريبية محلية إلى أن يوفّر المالك مشروع Firebase جديدًا أو يوافق على استخدام مشروع قائم.

ظهر تأكيد الحذف الرسمي لمشروع `device-streaming-c14af3c9`. يؤكد Firebase أن الحذف يشمل مشروع Google Cloud والخدمات وواجهات API المرتبطة به، مع إمكانية الاستعادة من قبل مالك المشروع خلال 30 يومًا. أذن المالك صراحة بحذف هذا المشروع تحديدًا لتحرير حد المشاريع.

أعيد فتح نافذة تأكيد الحذف بعد إغلاقها دون تنفيذ العملية في المحاولة الأولى. تحقق من صفحة الإعدادات أن المشروع لا يحتوي تطبيقات مسجلة ولا يفعّل فوترة؛ ما زال الحذف بانتظار تفعيل تأكيدات الحوار الأربعة وكتابة معرّف المشروع.

اكتمل حذف مشروع `device-streaming-c14af3c9` بنجاح بعد تأكيدات المستخدم. تعرض Firebase أن المشروع حُذف، وأصبح الحساب بلا مشاريع ظاهرة، مما يحرر مساحة لإنشاء مشروع `ChriGsm Demo` الجديد بلا فوترة.

تم إنشاء تطبيق Next.js جديد داخل `next/` على فرع `rebuild/next-firebase` من دون تعديل الواجهة المنشورة. يحتوي التطبيق على متجر وخدمات وكتالوج وتفاصيل طلب ومنطقة عميل ولوحة CMC، ويقرأ من بوابة بيانات موحدة تعود للبيانات التجريبية عندما لا توجد أسرار Firebase. نجح `npm run build` وتم توليد المسارات `/` و`/catalog` و`/service/[slug]` و`/account` و`/admin`.

أضيفت طبقات Firebase Client وFirebase Admin وقواعد Firestore وStorage وإعداد Firebase Emulator. أضيفت أوامر `npm run seed:demo` و`npm run clear:demo` التي تعتمد وسم `demo:true` حتى تحذف البيانات التجريبية فقط. نجح بناء التطبيق مجددًا بعد هذه الإضافات.

بعد حذف المشروع، يعرض Firebase Console مجددًا حد إنشاء المشاريع عند فتح نموذج مشروع جديد؛ يبدو أن حذف المشروع لا يحرر الحصة فورًا. يستمر التطبيق بالوضع المحلي التجريبي إلى حين زوال الحد أو منح حصة إضافية. في المقابل، شُغلت معاينة Next.js المحلية بنجاح على `http://localhost:3000` وظهرت الصفحة الرئيسية العربية وبطاقات خدمات GSM من البيانات التجريبية.

تم التحقق بصريًا من لوحة CMC في المعاينة الحية: مؤشرات العملاء والأرصدة والطلبات وبطاقات مجلدات التصنيفات تظهر من نفس البيانات التجريبية. كما تم فتح تفاصيل `TSL Tool Activation` وتأكيد ظهور السعر بالدرهم المغربي والحق الديناميكي للبريد ونموذج الطلب التجريبي.

تعذر دفع فرع `rebuild/next-firebase` إلى GitHub لأن رمز الوصول الحالي أعاد HTTP 403 عند محاولة الدفع. كما أعاد موصل Vercel 403 عند طلب قائمة أدوات النشر، رغم نجاحه سابقًا؛ لذا لا يمكن إنشاء رابط Preview خارجي من هذه الجلسة حاليًا. لم تُحذف أي ملفات محلية، وتبقى معاينة Next.js الحية عاملة محليًا.

## 2026-08-13 — CMC تفاعلية والتحقق

أضيفت واجهة CMC تفاعلية تعمل بالبيانات التجريبية محليًا: تبديل حالات الطلبات، عرض المنتجات على شكل مجلدات، إضافة خدمة تجريبية، إضافة تصنيف تجريبي، تعديل الأرصدة محليًا، وحذف متسلسل للتصنيف يتطلب كتابة اسمه. عُزل استيراد Firebase Admin عن مكوّنات المتصفح، ونجح `npm run build` باستخدام Webpack؛ لذلك ثُبّت `next build --webpack` كأمر إنتاج لتفادي عطل Turbopack الداخلي. المعاينة الحية تأكدت بصريًا على مسار `/admin` مع ظهور أقسام الإدارة، مؤشرات المحفظة، حالات الطلبات، والعملات بالدرهم المغربي.

## 2026-08-13 — تنفيذ صفحةً بصفحة: الغلاف المشترك

بدأت خطة التنفيذ المرحلية المعتمدة. تم تثبيت الفرع `rebuild/next-firebase` بحالة نظيفة وتحديث مكوّن `Header` و`BottomNav` ليبرزا المسار الحالي في سطح المكتب والهاتف، مع إبقاء روابط الخدمات والحساب وCMC والسلة. نجح `npm run build` بعد التعديل، وتحُقق بصريًا من الصفحة الرئيسية والكتالوج في المعاينة الحية. لم يُجر أي تعديل على فرع `main` أو الموقع المنشور الحالي.

## 2026-08-13 — تنفيذ صفحةً بصفحة: الصفحة الرئيسية

أعيد تنظيم `/` بوصفها واجهة متجر تطبيقية من دون Hero كبير. تحتوي على شريط بحث مباشر، وتصنيفات سريعة مع أعداد الخدمات، وخدمات GSM الأكثر طلبًا، وقسم متنوع للخدمات الرقمية، وبطاقة دعم. تُستمد الخدمات والتصنيفات من بوابة البيانات الموحدة وتبقى جميع الأسعار بالدرهم المغربي. نجح بناء الإنتاج باستخدام `npm run build`، ثم اكتمل الفحص المرئي للصفحة في المعاينة الحية حتى نهاية قسم المتنوع والدعم.

## 2026-08-13 — تصحيح البحث وتجربة الهاتف

استجابة لمراجعة الواجهة على الهاتف، أزيل زر البحث المتكرر من الرأس واستبدل رمز «الخدمات» في التنقل السفلي برمز كتالوج. حُولت صفحة `/catalog` من واجهة بحث تجريبية وأزرار فلترة غير وظيفية إلى مربع بحث واحد يعمل وفلاتر تصنيفات مع عدادات وحالة نتائج فارغة. نجح بناء الإنتاج، وتحقق البحث الحي بكلمة `Honor` فأظهر خدمة Honor FRP Service وحدها.

## 2026-08-13 — اكتمال نطاق المتجر الأولي

تضم الصفحة الرئيسية والكتالوج الآن تجربة منظمة للمتجر بلا تكرار في البحث. تبقى صفحة تفاصيل الخدمة مرتبطة بالحقول الديناميكية، وقد تحقق سابقًا إنشاء طلب TSL تجريبي وظهوره في منطقة العميل. سيكون التدفق نفسه مرتبطًا بـFirestore فور توفر Firebase؛ لذلك انتقل العمل إلى إعدادات حساب العميل والدعم الفني.

## 2026-08-13 — إعدادات العميل والدعم الفني

أضيفت إلى `/account` إجراءات عملية لإعدادات الحساب والدعم الفني. يستطيع العميل التجريبي تعديل الاسم والهاتف والبريد، مع حفظ محلي واضح، وفتح نموذج دعم يحفظ رسالة تجريبية في سجل الحساب بالحالة «مفتوح». لا تُحاكى كلمة المرور بشكل غير آمن؛ زر تغييرها معطل حتى يتاح Firebase Authentication لإرسال رابط تغيير حقيقي. نجح `npm run build`، وتحقق بصريًا من الإعدادات وحفظ رسالة الدعم في المعاينة الحية.

## 2026-08-13 — توسعة لوحة CMC

أضيف إلى CMC تبويبا الدعم والإعدادات، وعادت تبويبات الإدارة للظهور على الهاتف كمسار أفقي بدل اختفائها. تظهر رسالة دعم العميل في CMC ويمكن وضعها «تم الرد» محليًا. في الطلبات، يمكن إدخال كود تفعيل أو تفاصيل تسليم، ثم تغيّر العملية حالة الطلب إلى مكتمل وتظهر الكود. كما تعرض بطاقات العملاء عدد طلباتهم وزرًا لفتح طلبات عميل محدد، إلى جانب أزرار الرصيد التجريبية. نجح بناء الإنتاج وفُحصت التدفقات بصريًا في المعاينة الحية.

## 2026-08-13 — نشر Vercel مملوك

اكتمل بناء الإنتاج محليًا، ثم نُشرت نسخة Next.js مباشرةً في Vercel وجرى تحويلها، بموافقة المستخدم، إلى مشروع مملوك في فريق `arthur's projects`. الرابط الحي هو `https://temporary-speedy-jade-mdelya8.vercel.app`. فُحصت الصفحة الرئيسية ولوحة `/admin` المنشورتان وظهرتا دون أخطاء نشر؛ لوحة CMC تعرض تبويبات الطلبات والمنتجات والتصنيفات والعملاء والدعم والإعدادات. يبقى اسم المشروع والرابط مؤقتين من Vercel، ولا تصبح التحديثات التالية تلقائية حتى تستعاد صلاحية دفع GitHub أو يُربط المستودع بالمشروع.

## 2026-08-13 — صفحة الدخول التجريبية

أضيفت طبقة جلسة متصفح تجريبية في `src/lib/demo-auth.ts` ومسار `/login` عربي يتعرف إلى المدير والعميل عبر الهاتف أو البريد وكلمة المرور. تبقى بيانات الدخول التجريبية معزولة داخل المتصفح ولا تمثل حماية إنتاجية. نُفذ فحص البناء بنجاح، وفُحصت صفحة الدخول في المعاينة الحية: تعبئة حساب المدير ثم الدخول يحولان المستخدم إلى `/admin`، ويظهر اسم المدير في رأس الموقع.

## 2026-08-13 — حجب CMC وإدارة جلسة المدير

أضيفت بوابة `AdminAccessGate` إلى `/admin`: يسمح دور `admin` بعرض CMC فقط، بينما يرى المستخدم غير المسجل أو صاحب دور العميل شاشة وصول محمي مع رابط تسجيل دخول المدير. أضيفت كذلك بطاقة جلسة المدير في إعدادات CMC وتعرض هوية المدير التجريبية وخيار تسجيل الخروج. نجح `npm run build`، وفُحصت أربعة سيناريوهات في المعاينة: دخول المدير إلى CMC، الخروج إلى صفحة الدخول، منع المستخدم غير المسجل، ودخول العميل إلى الحساب ثم منعه من CMC.

## 2026-08-13 — إصدار إنتاج لتسجيل الدخول وحماية CMC

بعد تفويض المستخدم لأداة Vercel CLI، رُبط مجلد `next/` مباشرةً بمشروع `chrigsm-next` المملوك ونُشر إصدار الإنتاج للالتزامين `aa84d09` و`0096017`. أكّد Vercel جاهزية البناء وربط النطاق الإنتاجي السابق `https://temporary-speedy-jade-mdelya8.vercel.app` بالإصدار الجديد. فُحص رابط `/login` في الإنتاج وظهرت واجهة الدخول، ثم فُحص `/admin` بلا جلسة وظهرت شاشة الوصول المحمي بدل أدوات الإدارة.

## 2026-08-13 — تبسيط التنقل والسلة وجلسة العميل

أزيلت كل مداخل CMC من رأس المتجر وتنقل الهاتف. أصبح التنقل العلوي في الشاشات الكبيرة يضم «الرئيسية» و«الخدمات»، بينما تقود أيقونة الحساب غير المسجل إلى الدخول، والعميل إلى حسابه، والمدير إلى CMC. أضيفت سلة محمية في `/cart`: أيقونتها تحول غير المسجل إلى `/login?next=/cart` وتعرض السلة والمنتجات المقترحة للمسجل. أضيف كذلك زر تسجيل خروج العميل في منطقة الحساب، وفُحص ظهوره وإنهاء الجلسة والعودة إلى `/login` بنجاح. نجح بناء الإنتاج بعد إصلاح Suspense لقراءة مسار العودة في صفحة الدخول.

## 2026-08-14 — مسارات CMC الإدارية المحمية

- أضيفت مسارات خادمية أولية للتصنيفات والخدمات تحت `/api/admin` مع تحقق Firebase ID token ودور `admin` وسجل تدقيق خادمي.
- أضيف اختبار Firestore Emulator يحظر الكتابة المباشرة حتى من عميل المدير، ويتحقق من عزل العميل عن وثائق وطلبات عميل آخر؛ اجتاز الاختبار بنجاح بعد تصحيح توافق حزمة الاختبار.
- تم تضييق قواعد `categories` و`services` لمنع الكتابة المباشرة من المتصفح؛ ستنفذ CMC الكتابات عبر المسارات الخادمية فقط عند ربط الأزرار في المهمة التالية.
- نجح بناء Next.js محليًا، ورفض مسار `/api/admin/categories` الزائر غير الموثق بالاستجابة 403. بدأ اختبار واجهة العميل المحلي باستخدام حساب Firebase التجريبي، لاستكمال تحقق العميل والمدير قبل الالتزام والمعاينة.
- اكتمل فحص الواجهة المحلي: الزائر تلقى 403 من مسار التصنيفات الإداري، والعميل التجريبي سُجّل دخوله ثم مُنع من `/admin` برسالة دور واضحة، والمدير التجريبي وصل إلى CMC وشاهد بيانات Firestore الحية. لم تُنفذ أي كتابة اختبارية على بيانات Firestore الحقيقية.
- تم حذف سكربت اختبار API المحلي غير القابل للنقل فور فشل استخراج رمز Firebase كي لا يبقى أي اعتماد تجريبي داخل ملفات المشروع. يعتمد التحقق المعتمد على اختبار Emulator وقفل واجهة العميل/المدير وفحص مسار الزائر.
- نُشرت معاينة Vercel للالتزام `f4943ec` على `https://chrigsm-next-5elnlfufg-arthurs-projects-713d8d1f.vercel.app`. نجح فتح الصفحة الرئيسية عبر رابط اختبار Vercel المؤقت، ثم أعاد `/api/admin/categories` في المعاينة `{"error":"غير مصرح"}` للزائر كما هو مطلوب.
- واجه فحص HTTP العادي تحويل Vercel SSO (302) لأن معاينات المشروع محمية. لم تُعطّل الحماية؛ استُخدم رابط اختبار مؤقت للوصول إلى المعاينة، وينتهي تلقائيًا.
- اكتمل فحص العميل في معاينة Vercel: سجّل حساب العميل التجريبي الدخول ثم وصل إلى `/admin` ورأى رسالة «لوحة CMC للمدير فقط» بدل أي بيانات أو أدوات إدارة.
- وصل المدير التجريبي إلى CMC في معاينة Vercel، لكن الواجهة أعلنت «وضع تجريبي» بدل «Firebase متصل». السبب الظاهر هو أن متغيرات Firebase كانت مهيأة للإنتاج فقط، لذلك لا يمكن اعتماد هذه المعاينة لاختبار تدفق Firebase أو ترقية هذا الالتزام إلى الإنتاج بعده حتى تتاح بيئة معاينة متكافئة.
- نُشرت معاينة ثانية بعد نسخ إعدادات Firebase إلى Preview: `https://chrigsm-next-3y5qj38xk-arthurs-projects-713d8d1f.vercel.app`. نجح فتح الصفحة العامة وصفحة الدخول عبر رابط اختبار Vercel المحمي؛ بدأ فحص أدوار Firebase في هذه المعاينة المتكافئة.
- اكتمل فحص المدير في معاينة Firebase المتكافئة: سجّل المدير التجريبي الدخول، وصل إلى `/admin`، وظهرت CMC بعبارة «Firebase متصل» مع بيانات Firestore الحقيقية. وبذلك اجتازت المعاينة تحميل الصفحة العامة، رفض الزائر، منع العميل، ودخول المدير ببيئة Firebase.
- رُقّيت المعاينة الناجحة إلى الإنتاج. عيّن Vercel الرابط الدائم `https://temporary-speedy-jade-mdelya8.vercel.app` للإصدار الجديد، ونجح فتح الصفحة الرئيسية عليه ثم أعاد `/api/admin/categories` `{"error":"غير مصرح"}` للزائر. لم يظهر عطل إنتاجي يستلزم رجوعًا.

## 2026-08-14 — ربط أزرار CMC بالمسارات الخادمية

- حُوّلت واجهة التصنيفات والخدمات في CMC من عمليات React محلية إلى نماذج مرتبطة بمسارات `/api/admin/categories` و`/api/admin/services` الموثقة. لا يقدم وضع عدم اتصال Firebase تعديلًا محليًا بديلًا.
- أضيفت نماذج إنشاء/تعديل للخدمات والتصنيفات، مع تحديث حالة الواجهة فقط من كائن الاستجابة الخادمية، وحذف متسلسل يظل محميًا بتأكيد الاسم ومنع الخدمات المرتبطة بطلبات.
- نجح `npm run build` و`npm run test:firestore-rules`. عند فتح CMC محليًا بحساب المدير، ظهرت عبارة «Firebase متصل» و«الطلبات والتصنيفات والخدمات تحفظ خادميًا» بوضوح. يبدأ اختبار إضافة وحذف بيانات تحقق مؤقتة لا ترتبط بأي عميل.
- نجح اختبار واجهة CMC الفعلي بحساب المدير: أُنشئ تصنيف تحقق مؤقت عبر النموذج، وأعادت الواجهة رسالة نجاح خادمية وظهرت البطاقة الجديدة بعد الاستجابة مباشرةً. التصنيف موسوم للتحقق فقط وسيزال بعد اختبار الخدمة والحذف المتسلسل.
- نجح اختبار الخدمة الفعلي في CMC: أُضيفت «خدمة تحقق CMC مؤقتة» تحت تصنيف التحقق، وعادت رسالة نجاح خادمية وظهرت الخدمة بسعرها في الواجهة. ستعدل ثم تزال مع تصنيفها المؤقت للتحقق من جميع العمليات من دون ترك بيانات اختبار.
- نجح اختبار تعديل الخدمة: غُيّر السعر من 1 إلى 2 د.م. عبر النموذج، وعادت رسالة نجاح من Firebase وظهرت القيمة الجديدة في بطاقة الخدمة. يتبقى حذف التصنيف المؤقت للتحقق من الحذف المتسلسل ثم تنظيف بيانات الاختبار.
- اكتمل اختبار الحذف المتسلسل: بعد إدخال اسم التصنيف صراحةً، حذف CMC تصنيف التحقق وخدمته المؤقتة (1) عبر Firebase. اختفت عناصر التحقق من الواجهة ولم تبق أي بيانات اختبار أُنشئت في هذا التشغيل.
- فُحص العميل التجريبي بعد التعديل: ظهر له منع CMC كما يجب. اكتُشف نص قديم غير دقيق في بوابة الواجهة يوحي بأن الحماية «تجريبية» رغم عمل Firebase وCustom Claims؛ سيصحح في هذا الالتزام لأن الحقيقة التشغيلية تمنع هذا الوصف.
- بعد إعادة تحميل بوابة CMC، ظهر النص المصحح للعميل الممنوع: الواجهة تتحقق من جلسة Firebase، والمسارات تتحقق خادميًا من ID Token وCustom Claim. لا تبقى عبارة «حماية تجريبية» في هذا المسار.
- اجتاز Smoke test للمدير في معاينة Vercel: سجل المدير الدخول وظهرت CMC برسالة «Firebase متصل» ونص يوضح أن التصنيفات والخدمات تحفظ خادميًا. تُفحص الآن عناصر التصنيفات في المعاينة قبل ترقية الإنتاج.
- نُشرت واجهة CMC المرتبطة بالخادم إلى الإنتاج. نجح فتح الرابط الدائم `https://temporary-speedy-jade-mdelya8.vercel.app`، وأعاد `/api/admin/categories` في الإنتاج `{"error":"غير مصرح"}` للزائر؛ لم يظهر عطل يستلزم رجوعًا.

## 2026-08-14 — أرصدة العملاء وسجل التدقيق الخادمي

- أضيف مسار محفظة إداري ذري يحدّث `customers.walletMad` وينشئ قيد `walletEntries` وسجل `auditLogs` في المعاملة نفسها، مع تحقق المدير وسبب إلزامي ومنع الرصيد السالب.
- عُدلت قواعد Firestore لمنع أي كتابة مباشرة إلى العملاء، بما في ذلك من متصفح المدير، وأضيفت اختبارات Emulator لعزل قيود المحفظة وحظر كتابة الرصيد والسجل مباشرة.
- اجتاز `npm run build` و`npm run test:firestore-rules`. في اختبار CMC المحلي، رُفضت محاولة خصم 50 د.م. من حساب عمر ذي الرصيد الصفري برسالة واضحة ولم يتغير الرصيد أو ينشأ قيد.
- رفض المسار المحلي طلب الزائر وطلب عميل Firebase الفعلي بالاستجابة 403. نجح المدير في إضافة 50 د.م. إلى حساب عمر ذي الرصيد الصفري ثم خصمها، فعاد الرصيد إلى 0 د.م.
- فحص قراءة فقط أكد وجود قيدي المحفظة المتقابلين (+50 و-50) وسجلي `wallet_adjusted` المطابقين لهما، مع أرصدة سابقة ولاحقة صحيحة. حُذفت سكربتات التحقق المؤقتة بعد الاختبار.
- اجتازت معاينة Vercel تحميل المتجر ودخول المدير الحقيقي؛ ظهرت بطاقة العملاء بحقل سبب إلزامي وأزرار +50/-50، ورسالة أن الأرصدة تحفظ خادميًا.
- أكدت معاينة Vercel أن CMC للمدير تعرض حقل سبب التعديل وأزرار المحفظة المتصلة بـFirebase. فحص المسار من دون مشاركة وصول Vercel وصل إلى طبقة حماية Vercel (302 إلى SSO)، لا إلى التطبيق؛ لا يمثل ذلك استجابة API أو فشلًا أمنيًا في المسار. فحوص المسار الفعلية للزائر والعميل والمدير نجحت محليًا ضد Firebase.
- نُشرت محفظة العملاء المدققة إلى الإنتاج. نجح تحميل الرابط الدائم `https://temporary-speedy-jade-mdelya8.vercel.app`، وأعاد طلب POST غير موثق إلى مسار المحفظة الاستجابة 403؛ لم يظهر عطل يستلزم رجوعًا.
