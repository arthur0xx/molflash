# نتائج Smoke Test للمعاينة — 82f4bb5

- **المعاينة:** `https://chrigsm-next-lznimx970-arthurs-projects-713d8d1f.vercel.app`
- **حالة النشر:** READY على Vercel، من الفرع `rebuild/next-firebase` والالتزام `82f4bb5`.
- **الواجهة العامة:** الصفحة الرئيسية تُحمّل بنجاح، وتعرض تصنيفًا نشطًا واحدًا فقط هو «أدوات التفعيل» بعدد 19 خدمة. لم يظهر أي تصنيف فارغ في الواجهة.
- **بطاقات الخدمات:** بطاقات AMT الأربع ظاهرة، وكل بطاقة رابط كامل إلى صفحة الخدمة مع المدة والسعر بالدرهم المغربي.
- **واجهة الدخول:** تعمل وتعرض دخول البريد وكلمة المرور، إنشاء الحساب، المتابعة عبر Google، واستعادة كلمة المرور. كما تظهر رسالة واضحة بأن التسجيل عبر Google للمرة الأولى ينشئ ملف الحساب تلقائيًا.
- **حدود الاختبار:** لم يتم تنفيذ تسجيل دخول أو تجربة ربط حساب Google، تفاديًا لتعديل حالة حسابات المالك أو أي بيانات حقيقية.

## نتائج الحماية وCMC

| الفحص | النتيجة | الدليل |
|---|---:|---|
| وصول زائر إلى `/api/admin/snapshot` | ناجح — `403` | `{\"error\":\"غير مصرح\"}` من التطبيق بعد تجاوز حماية المعاينة المؤقتة. |
| إرسال `POST /api/orders` بلا جلسة | ناجح — `401` | `{\"error\":\"أكد بريدك الإلكتروني ثم سجّل الدخول لإرسال الطلب\"}`. لم يُنشأ أي طلب. |
| بوابة كود التسليم في CMC | ناجح | مكوّن `OrderGrid` يعرض صندوق التسليم فقط عندما تكون `order.status === \"processing\"`، ويُبقي الكود الحالي للقراءة عند وجوده. |

**خلاصة المرحلة:** اجتازت معاينة الالتزام `82f4bb5` اختبارات الواجهة العامة، الدخول، تصفية التصنيفات، وحماية المسارات الحساسة. لا توجد ملاحظات حاجبة للترقية إلى الإنتاج.


## ترقية الإنتاج

بدأت ترقية الالتزام `82f4bb5` إلى الإنتاج في Vercel عبر نشر جديد بالمعرّف `BB8xjGNPuRc29sgpZMzH8emcN8eC`. سيُربط بالنطاق الأساسي `temporary-speedy-jade-mdelya8.vercel.app` بعد اكتمال البناء. وقت التسجيل كانت الحالة `Building` ولم تُسجل أي أخطاء في السجل المعروض.
في متابعة البناء، وصل Vercel إلى خطوة `Running TypeScript` دون ظهور خطأ حتى الآن؛ لذلك يستمر الانتظار قبل إجراء فحص النطاق الأساسي.

**نتيجة ترقية الإنتاج:** اكتمل النشر `BB8xjGNPuRc29sgpZMzH8emcN8eC` بحالة `Ready` خلال 40 ثانية، وأصبح الالتزام `82f4bb5` على النطاق الأساسي `https://temporary-speedy-jade-mdelya8.vercel.app`. فحص النطاق بعد الترقية أكد تقديم HTML الإصدار الجديد نفسه عبر معرّف النشر `dpl_BB8xjGNPuRc29sgpZMzH8emcN8eC`، مع ظهور تصنيف أدوات التفعيل و19 خدمة نشطة.

**تحقق خدمة EFT Pro السنوية:** تعرض صفحة الإنتاج `/service/eft-pro-tool-12-months` صورة Cloudinary الصحيحة، وسعر `650 د.م.`، وحقل البريد الإلكتروني لحساب EFT Pro كحقل مطلوب وملاحظة اختيارية فقط. لم يُرسل نموذج ولم يُنشأ طلب اختبار.
