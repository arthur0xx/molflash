# سجل التقدم — مركز الدفع اليدوي

| التاريخ | الإجراء | النتيجة |
|---|---|---|
| 2026-08-18 | توسيع `StoreSnapshot` ومصدر CMC | تضمين `paymentMethods` و`payments` في لقطة المالك فقط، وإفراغها للمديرين المحدودين. |
| 2026-08-18 | إنشاء مسارات الدفع | إضافة CRUD محمي للمالك، إنشاء مرجع تحويل خادمي للعميل، ومراجعة ذرية تؤكد الطلب أو تشحن المحفظة. |
| 2026-08-18 | إنشاء مركز الدفع CMC | تبويب «إدارة الدفع» للمالك فقط مع مسودات CMI/Payzone، وإجراءات وضع قيد المراجعة أو التأكيد أو الرفض. |
| 2026-08-18 | ربط الطلب | بعد إنشاء الطلب، يختار العميل وسيلة مفعلة ويحصل على تعليمات ومرجع فريد؛ منع المسارات من تكرار تحويل مفتوح للطلب. |
| 2026-08-18 | ربط شحن الرصيد | يستطيع العميل إنشاء تحويل لشحن الرصيد ومتابعة عمليات التحويل المعلقة؛ الرصيد لا يتغير من العميل. |
| 2026-08-18 | فحص محلي | `npm run lint` ناجح بلا تحذيرات؛ `npm run build` ناجح وظهرت مسارات الدفع ضمن ناتج التطبيق. |

## المرحلة التالية

تحديث قواعد Firestore وملف اختبار المحاكي للمجموعتين `paymentMethods` و`payments`، ثم تشغيل اختبار القواعد قبل معاينة النشر.
| 2026-08-18 | قواعد Firestore والمحاكي | أضيفت سياسات deny-by-default لـ `paymentMethods` و`payments`. نجح `npm run test:firestore-rules` مع عزل العميل، منع المدير المحدود من SDK، ومنع الكتابات المباشرة. |
| 2026-08-18 | CMC | اكتملت واجهة المالك لإنشاء/تعديل/تعطيل/حذف وسيلة غير مستخدمة ومراجعة التحويلات؛ تبقى محجوبة عن المديرين. |
| 2026-08-18 | فحص نهائي محلي | نجح `git diff --check` و`npm run lint` و`npm run build`. |
| 2026-08-18 | معاينة Vercel | الالتزام `b362660` نُشر بنجاح في المعاينة: `https://chrigsm-next-6rriyj7uu-arthurs-projects-713d8d1f.vercel.app`. تم التحقق بصريًا من الصفحة الرئيسية وصفحة خدمة، وتعرض صفحة الخدمة خطوة اختيار التحويل اليدوي بعد إنشاء الطلب. |

## ملاحظة النشر

المعاينة جاهزة، بينما قواعد Firestore الجديدة موجودة في المستودع وتحتاج تنفيذ عملية نشر قواعد مستقلة إلى مشروع Firebase قبل ترقية التغييرات إلى الإنتاج.
| 2026-08-18 | مصادقة Firebase | سجّل المستخدم الدخول إلى حساب Google المصرح له؛ صفحة Firebase CLI تعرض معرّف الجلسة المطابق وتنتظر تأكيد الربط فقط. لا تُحفظ رموز التفويض في السجل. |
| 2026-08-18 | نشر قواعد Firestore | تحقق Firebase الرسمي من القواعد دون أخطاء، ثم نُشرت خدمة `firestore` فقط إلى مشروع `chrigsm-4f883` بنجاح (100%). |
| 2026-08-18 | اختبار API غير موثق | مسار المعاينة `/api/payments` رفض الطلب غير الموثق برسالة عربية تطلب تأكيد البريد وتسجيل الدخول، ولم يكشف أي وسيلة أو سجل دفع. |
| 2026-08-18 | إثبات التحويل اليدوي | أضيفت حالة `proof_submitted` وبيانات إثبات مقيّدة داخل `payments` من دون رابط عام، ومسار عميل لتوقيع الرفع وتثبيت الإثبات بعد تحقق الملكية والحالة والنوع والحجم. |
| 2026-08-18 | واجهة الإثبات والمطابقة | يستطيع العميل إرفاق PNG/JPEG/WebP حتى 10 MiB بعد ظهور المرجع، ويرى المالك الإثبات عبر مسار محمي ويكتب ملاحظة مطابقة بنكية إلزامية قبل التأكيد. |
| 2026-08-18 | فحص إثبات التحويل | نجح `npm run lint` بعد ربط تدفق الطلب وشحن الرصيد وCMC. |
| 2026-08-18 | إشعار WhatsApp لإثبات الدفع | أضيف حدث خادمي idempotent للمالك عند تثبيت الإثبات. لا يرسل أي رسالة إلا عند وجود إعداد Meta الرسمي والقالب `WHATSAPP_TEMPLATE_ADMIN_PAYMENT_PROOF_UPLOADED` ورقم المالك في أسرار Vercel. |
| 2026-08-18 | معاينة إثبات التحويل | معاينة الالتزام `3b48132` أصبحت READY على Vercel واستجابت الصفحة الرئيسية بنجاح عبر رابط وصول محمي مؤقت. تعذر اختبار API غير الموثق مباشرة من المتصفح قبل ضبط وصول المعاينة لأن حماية Vercel اعترضت المسار؛ اختبارات Firebase المحلية تغطي عزل البيانات ومنع الكتابات المباشرة. |
| 2026-08-18 | تحقق نهائي محلي | بعد إصلاح إرسال نوع تسليم Cloudinary ضمن الطلب الموقّع، نجح `npm run lint` و`npm run build` مجددًا. |
| 2026-08-18 | تحقق معاينة Vercel | معاينة الالتزام `3b48132` أصبحت READY: `https://chrigsm-next-a3iar771q-arthurs-projects-713d8d1f.vercel.app`. الاستجابة العامة كانت 200، مع رؤوس حماية CSP وHSTS و`nosniff`. |
