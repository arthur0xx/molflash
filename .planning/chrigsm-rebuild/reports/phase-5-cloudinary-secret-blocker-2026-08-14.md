# تقرير تشغيل ChriGsm

| البند | النتيجة |
|---|---|
| التاريخ والتوقيت | `2026-08-14 GMT+1` |
| المرحلة | `5 — صور بلا فوترة` |
| المهمة الذرية | تسجيل إعداد Cloudinary القائم في Vercel ثم محاولة استكمال متغيرات الرفع الموقّع. |
| الحالة | `محجوب جزئيًا` |
| الملفات المتغيرة | سجلات التخطيط وتقرير العائق فقط؛ لا تغيير في كود التطبيق أو قواعد Firebase. |

## بوابات التحقق

| البوابة | النتيجة | الدليل |
|---|---|---|
| البناء | غير مطلوب | لا يوجد تغيير في الكود؛ آخر بناء ناجح موثق للالتزام `f43319b`. |
| قواعد Firebase | غير مطلوب | لم تتغير أي قاعدة أو بيانات Firestore. |
| الزائر | غير مطلوب | لم ينشر إصدار جديد؛ حماية مسار التوقيع موثقة سابقًا. |
| العميل | غير مطلوب | لم ينشر إصدار جديد. |
| المدير | ناجح جزئيًا | فتحت جلسة Cloudinary بيئة `tkkrn30h` وظهر زوج API نشط قائم باسم `Root`. |
| العزل | غير مطلوب | لم يتغير أي مسار أو نطاق بيانات. |
| المعاينة | غير منشور | لا يمكن اختبار التوقيع حتى يوجد `CLOUDINARY_API_SECRET`. |
| الإنتاج | غير مرقّى | لا نشر جديد؛ الرابط الدائم يبقى على الإصدار السليم السابق. |

## الإصدار

| العنصر | القيمة |
|---|---|
| الالتزام المحلي | لم ينشأ بعد لهذا التوثيق |
| رابط المعاينة | غير منشور |
| رابط الإنتاج | `https://temporary-speedy-jade-mdelya8.vercel.app` |
| آخر إصدار سليم | `f43319b` — بنية الرفع الموقّع مشروطة بالتهيئة. |

## التالي أو العائق

تم تخزين `CLOUDINARY_CLOUD_NAME` و`CLOUDINARY_API_KEY` للزوج القائم في Vercel كقيم حساسة ضمن بيئتي Preview وProduction. يبقى `CLOUDINARY_API_SECRET` مفقودًا. تعرض Cloudinary السر للقيمة القائمة بشكل مخفي ولا تتيحه للاسترجاع من صفحة API Keys. يمنع بروتوكول المشروع إنشاء أو تدوير سر جديد، لذلك يتوقف الرفع الموقّع حتى يضيف مالك Cloudinary السر القائم، أو ينشئ زوجًا جديدًا بنفسه خارج مهمة التنفيذ ثم يضع السر في Vercel.

لا يُرسل السر عبر المحادثة أو Git أو متغيرات المتصفح. فور ظهوره في بيئة Vercel الخادمية فقط، تستأنف المهمة بنشر معاينة واختبار `GET` و`POST` لمسار التوقيع كزائر وعميل ومدير، ثم رفع صورة تحقق وعرضها في الكتالوج قبل أي ترقية إنتاجية.
