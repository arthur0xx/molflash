# ملاحظات تدقيق فهرسة ChriGsm — 19 أغسطس 2026

## فحص الإنتاج

- يعرض `https://temporary-speedy-jade-mdelya8.vercel.app/robots.txt` حاليًا السماح بالزحف إلى `/` و`/catalog` و`/service/`، ويمنع `/admin` و`/account` و`/cart` و`/login` و`/api/`.
- لم تُمنع صراحةً من الزحف مسارات تحقق المستخدم مثل `/verify-email` و`/phone-verification`، ولا يظهر `/blog/` في مصفوفة السماح الصريحة، رغم أن السماح العام `/` يغطيه.
- تعرض `https://temporary-speedy-jade-mdelya8.vercel.app/sitemap.xml` حاليًا الصفحة الرئيسية والكتالوج وفهرس المدونة و32 صفحة خدمة عامة. لا تتضمن الخريطة المسارات الخاصة، وهو سلوك صحيح.
- تسجل الصفحة الرئيسية والكتالوج والمدونة وصفحات الخدمات `lastmod` متطابقًا وقت إنشاء خريطة الموقع، لا وقت تغير المحتوى الفعلي. أما مقالات المدونة المنشورة فتستخدم تواريخ المقال عند توفرها.

## نتيجة أولية

البنية الأساسية تسمح بفهرسة الصفحات التجارية العامة فقط عبر خريطة الموقع، لكن يجب إضافة `noindex` على مستوى Metadata للصفحات الخاصة بدل الاعتماد على robots وحده، وتوسيع قائمة المنع لتشمل مسارات التحقق والتدفقات غير القابلة للبحث.

## رصد أولي للمنافسة والمحتوى

- يركز المنافس المحلي `gsmserver.ma` على معدات الورش المادية، مع رسائل ثقة وضمان وشحن داخل المغرب وأسئلة شائعة.
- يعرض `info-gsm.com` و`mechanic-sunshine.com` كتالوجات واسعة لقطع وأدوات الهاردوير وملحقات الإصلاح؛ وهي منافسة تجارية مجاورة وليست تطابقًا كاملًا لخدمات ChriGsm الرقمية.
- المحتوى العربي المرجعي حول برامج المخططات يجمع مباشرة بين BORNEO وJCID وZXW وXZZ مع شرح الاستخدام للفني، وهو مؤشر واضح على قابلية بناء مقالات مقارنة وتعليمية مرتبطة بالخدمات الرقمية المتاحة في ChriGsm.
- فرصة ChriGsm التفاضلية هي المحتوى العربي الموثوق والمحدّث لفنيي الصيانة حول **اختيار وتفعيل واستخدام أدوات البرمجيات والمخططات**، بدل محاولة منافسة متاجر الأدوات المادية في صفحات عامة لا تطابق الكتالوج.

### مصادر الرصد

1. https://gsmserver.ma/
2. https://info-gsm.com/
3. https://mechanic-sunshine.com/
4. https://i2cteam.com/blog/-mobile-repair-schematics-software

## نمط المحتوى المتكرر في نتائج البحث

- صفحة المقارنة القابلة للمنافسة لا تكتفي بتعريف الأداة: تعرض قرارًا سريعًا حسب حالة الاستخدام، وجدول مقارنة، وقسمًا لكل أداة يضم نقاط القوة والقيود ولمن تناسب، ثم توصيات مرتبطة بطرازات الأجهزة أو مسار العمل.
- المحتوى الذي ينجح لفنيي الصيانة يطابق النية العملية: مقارنة أداة، اختيار اشتراك، شرح ميزة، أو تشخيص مسار عطل. لا ينبغي أن يحوّل ChriGsm إلى موقع لتنزيل البرامج أو تجاوز حقوق المنتجات.
- يوجد دليل رسمي لـ ZXW للتنزيل، ويمكن استخدام روابط المواقع الرسمية للمصادر داخل المقالات؛ لا تُستضاف الملفات التنفيذية ولا تُعاد توجيه المستخدم إلى مصادر غير موثقة.

### مصادر إضافية

5. https://kewalmobile.com/guides/schematic-tools-comparison
6. https://zxwtools.com/en/download.html
7. https://www.waseem-tech.com/2025/09/zxw.html

## تحقق المعاينة بعد التنفيذ

- نُشرت معاينة الالتزام `266787e` بحالة `READY` على Vercel.
- فُتحت `/login` في المعاينة بصورة طبيعية، وعرضت محتوى تسجيل الدخول المتوقع.
- أظهر DOM الفعلي للصفحة وسم `meta[name="robots"]` بالقيمة `noindex, nofollow`، وهو التوجيه المطلوب لاستبعادها من نتائج البحث بعد أن يتمكن Google من زحفها.

تؤكد معاينة `/blog` العنوان المحدث «مدونة صيانة الهواتف وأدوات GSM للفنيين | ChriGsm». كما أظهر DOM أن توجيه robots هو `index, follow` وأن بيانات `Blog` المنظمة موجودة، لذلك تبقى صفحة المدونة العامة قابلة للفهرسة بينما صفحات الحساب مستبعدة.

تعرض معاينة `robots.txt` السماح العام بالزحف مع منع `/api/` فقط، وهو متوافق مع الاعتماد على `noindex` لمسارات الحساب. كما فُتحت صفحة خدمة Unlock Tool العامة بعنوان وصفي وسعر ومدة معالجة وحقول طلب حقيقية، وتبقى مناسبة للفهرسة كصفحة خدمة؛ أضيف لها Schema خدمة ومسار تنقّل في الإصدار نفسه.

## حالة الترقية إلى الإنتاج

بعد تأكيد المالك، فُتحت قائمة إجراءات المعاينة `dpl_a7PzQ5WJzsyiZWsAKTSYZQo26oUz` وتم اختيار ترقية الإنتاج من واجهة Vercel. ستُتحقق حالة النطاق العام مباشرة بعد اكتمال عملية الترقية.
اكتمل اختيار إجراء «Promote to Production» للمعاينة المؤكدة من واجهة Vercel بعد تعذر تفاعل القائمة التقليدي؛ الخطوة التالية هي متابعة بناء الإنتاج والتحقق من النطاق العام.
تم نشر ترقية الإنتاج بنجاح على Vercel بحالة `Ready`، وربط الإصدار `d54b4fc` بالنطاق العام `temporary-speedy-jade-mdelya8.vercel.app`.
تم فحص `/login` على النطاق الإنتاجي بعد الترقية، ويعرض DOM الفعلي توجيه `noindex, nofollow` كما هو مطلوب.
